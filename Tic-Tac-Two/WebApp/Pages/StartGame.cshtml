@page
@using Domain
@model WebApp.Pages.StartGameModel

@{
    ViewData["Title"] = "Start Game";
}

<h1>Start Game</h1>

<h4>@Model.GameName</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <label for="enterGameButtons" class="control-label">Enter as ...</label>
        <div id="enterGameButtons" class="form-group">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <form method="post">
                <div class="form-group">
                    <input type="submit" name="PlayerXName" value="@Model.PlayerXName" class="btn btn-dark btn-lg"/>
                    <input asp-for="Password" type="hidden" value="@Model.PlayerXPassword"/>
                    <input asp-for="GameName" type="hidden" value="@Model.GameName"/>
                    @* <input type="password" asp-for="Password" /> *@
                    @* <button type="submit">Start Game</button> *@
                </div>
                @* <div class="form-group"> *@
                @*     <label for="playerXLink" class="control-label">Copy link for @Model.PlayerXName:</label> *@
                @*     <div id="playerXLink">@Url.RouteUrl(ViewContext.RouteData.Values)</div> *@
                @* </div> *@
                @* <label for="basic-url">Your vanity URL</label> *@
                @* <div class="input-group mb-3"> *@
                @*     <div class="input-group-prepend"> *@
                @*         <span class="input-group-text" id="basic-addon3">https://example.com/users/</span> *@
                @*     </div> *@
                @*     <input type="text" class="form-control" id="basic-url" aria-describedby="basic-addon3"> *@
                @* </div> *@
            </form>
            <form method="post">
                <div class="form-group">
                    <input type="submit" name="PlayerOName" value="@Model.PlayerOName" class="btn btn-dark btn-lg"/>
                    <input asp-for="Password" type="hidden" value="@Model.PlayerOPassword"/>
                    <input asp-for="GameName" type="hidden" value="@Model.GameName"/>
                </div>
            </form>
        </div>
    </div>
</div>

<div>
    <a asp-page="LoadGame">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
